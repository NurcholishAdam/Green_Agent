apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: green-agent-test
namePrefix: dev-

resources:
- ../../base

# ✅ This sets the image for all containers in the base resources
images:
- name: green-agent
  newTag: test
