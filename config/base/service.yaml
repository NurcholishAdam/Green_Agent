apiVersion: v1
kind: Service
metadata:
  name: green-agent
  namespace: green-agent
spec:
  type: ClusterIP
  selector:
    app: green-agent
  ports:
  - name: api
    port: 8000
    targetPort: 8000
  - name: ray-dashboard
    port: 8265
    targetPort: 8265
