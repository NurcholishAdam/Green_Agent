apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: green-agent

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ray-cluster.yaml

commonLabels:
  app: green-agent
