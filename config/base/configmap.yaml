# config/base/configmap.yaml - CORRECT
apiVersion: v1
kind: ConfigMap
metadata:
  name: green-agent-config
  labels:
    app: green-agent
data:
  green_agent_config.yaml: |
    system:
      version: "5.0.0"
      mode: "unified"
    # ... config data only, NO probe fields
