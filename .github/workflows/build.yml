name: Build and Push Green Agent

on:
  push:
    branches: [main, develop]
    tags:
      - 'v*'
  pull_request:
    branches: [main]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

permissions:
  contents: read
  packages: write
  security-events: write

jobs:
  build-and-scan:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Log in to Container Registry
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Build and push
      uses: docker/build-push-action@v5
      with:
        context: .
        file: ./Dockerfile
        push: true
        tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
        cache-from: type=gha

    # ✅ FIX 1: Trivy with correct SARIF output path + handle empty results
    - name: Run Trivy vulnerability scanner
      uses: aquasecurity/trivy-action@master
      with:
        image-ref: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
        format: 'sarif'
        output: 'trivy-results.sarif'  # ✅ Explicit output file
        severity: 'CRITICAL,HIGH'
        exit-code: '0'  # ✅ Don't fail workflow if no vulnerabilities
        ignore-unfixed: true

    # ✅ FIX 2: Only upload SARIF if file exists (handles empty results)
    - name: Upload Trivy scan results to GitHub
      uses: github/codeql-action/upload-sarif@v4  # ✅ Updated to v4
      with:
        sarif_file: 'trivy-results.sarif'
      if: always() && hashFiles('trivy-results.sarif') != ''

    # ✅ FIX 3: Fallback - create empty SARIF if Trivy found nothing
    - name: Create empty SARIF if no vulnerabilities found
      if: always() && hashFiles('trivy-results.sarif') == ''
      run: |
        echo "⚠️ No vulnerabilities found or SARIF not generated - creating empty report"
        cat > trivy-results.sarif << 'EOF'
{
  "$schema": "https://raw.githubusercontent.com/oasis-tcs/sarif-spec/master/Schemata/sarif-schema-2.1.0.json",
  "version": "2.1.0",
  "runs": [{
    "tool": {
      "driver": {
        "name": "Trivy",
        "informationUri": "https://trivy.dev/",
        "version": "0.0.0"
      }
    },
    "results": []
  }]
}
EOF

    # ✅ FIX 4: Upload SARIF after ensuring file exists
    - name: Upload Trivy scan results to GitHub (retry)
      uses: github/codeql-action/upload-sarif@v4
      with:
        sarif_file: 'trivy-results.sarif'
      if: always()
