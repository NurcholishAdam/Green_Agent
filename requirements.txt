# Core dependencies
fastapi>=0.104.0
uvicorn>=0.24.0
pyyaml>=6.0.0
aiohttp>=3.9.0
ray>=2.7.0
kubernetes>=28.0.0
prometheus-client>=0.19.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# ---------------------------------------------------------------------------
# Dependencies for advanced enhancements (LIMIT Graph, MODP, RLHF,
# Multi‑Teacher On‑Policy Distillation, Bio‑inspired Optimisation, MoE expert gating, FlexGen)
# ---------------------------------------------------------------------------

# Numerical and scientific computing
numpy>=1.24.0
scipy>=1.10.0

# Historical ML teachers and model serialization (RandomForest, etc.)
scikit-learn>=1.3.0
joblib>=1.3.0

# Data handling for interaction logs and state vectors (CSV/JSON)
pandas>=2.0.0

# Schema validation and settings management (Pydantic v2)
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Zero Trust security: encryption, tokens, authentication
cryptography>=41.0.0
pyjwt>=2.8.0

# Retry logic with exponential backoff (circuit breakers)
tenacity>=8.2.0

# Asynchronous file I/O (persistence of routing logs, Q‑weights, etc.)
aiofiles>=23.0.0

# Network analysis for LIMIT Graph (graph metrics, centrality)
networkx>=3.1
